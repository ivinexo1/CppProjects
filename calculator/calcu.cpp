#include <iostream>

using namespace std;

int main() {
    char comm;
    int a, b;

    cout << "num 1, command, num 2 \n";
    cin >> a >> comm >> b;

    do {
        swap(comm) {
            case '+' 
